{
  "name": "webnote",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "terser": "^5.44.1",
    "turbo": "^1.10.0"
  },
  "packageManager": "pnpm@8.7.0"
}